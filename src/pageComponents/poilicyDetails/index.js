import React from 'react';
import { Card, CardContent, Grid, Typography } from '@mui/material';

const PolicyDetails = ({ data }) => {
  return (
   <Grid container>
       <Typography>Policy detail coming soon ...</Typography>
     </Grid>
  );
};

export default PolicyDetails;
